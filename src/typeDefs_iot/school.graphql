input CreateSchoolInput{
  name: String!
}

type School{
  _id: ID!
  name: String
  isActive: Boolean!
  createdAt: Float!
  updatedAt: Float!
}

type Query{
  schools: [School]
}

type Mutation{
  createSchool(input:CreateSchoolInput!): School
}